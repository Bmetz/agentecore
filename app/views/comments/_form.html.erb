<% 
#locals
comment ||= @comment
%>

<div id="new_comment" class="hidden">    
	<% less_remote_form_for(:comment, :url => profile_comments_path(@profile), :html => { :id => "#{dom_id(@profile)}_comment_form"} ) do |f| %>
	<div id="thickbox_form">
    <%= f.text_area :comment, :label => t(:comment_label)  %>
    <%= t(:youtube_label) %>
		<div class="row button"><%= f.submit t(:option_add), :class=>"button" %></div>
		<div class="clear"></div>
    </div>
	<% end %>
</div>
